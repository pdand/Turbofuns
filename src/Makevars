PKG_FCFLAGS = $(SHLIB_OPENMP_FFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_FFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
SHLIB_LD = $(SHLIB_FCLD)
SHLIB_LDFLAGS = $(SHLIB_FCLDFLAGS)
SHLIB = polyACM20200617$(SHLIB_EXT)

all:  $(SHLIB)
	 
$(SHLIB): $(FT_OBJS) $(C_OBJS)
$(SHLIB_LINK): -o $ $(OBJECTS) $(ALL_LIBS) 

